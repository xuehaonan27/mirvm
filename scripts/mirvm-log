#!/usr/bin/env bash
set -euo pipefail

root=$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)
if [ -n "${MIRVM:-}" ]; then
    bin=$MIRVM
elif [ -x "$root/target/release/mirvm" ]; then
    bin=$root/target/release/mirvm
else
    bin=mirvm
fi

exec "$bin" log "$@"
