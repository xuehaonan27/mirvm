[package]
name = "cless_doctest_contract"
version = "0.1.0"
edition = "2024"
build = "build.rs"

[dev-dependencies]
doctest-helper = { path = "doctest-helper" }

[profile.test]
opt-level = 1
debug-assertions = false
overflow-checks = false
